package ru.incube.sleuth.data.recognizers;

import ru.incube.sleuth.data.IDataExtractor;

import java.nio.file.Path;

public class FileTypeRecognizer {
    /*
    Identifies file types within a directory to delegate parsing responsibilities correctly.
    recognizeFileType(Path path)
     */
    public IDataExtractor recognizeFileType(Path path) {
        throw new UnsupportedOperationException("Not implemented yet");
    }
}
